@{
    ViewBag.Title = "Home Page";
}
@(Html.Kendo().TabStrip()
    .Name("tabstrip")
    .Items(tabs =>
    {
        tabs.Add().Text("First").Content("Select the 'Grid' tab");
        tabs.Add().Text("Grid").LoadContentFrom("Grid", "Home");
    })
)
