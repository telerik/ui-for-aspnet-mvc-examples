@model TimeSpan?

@(
    Html.Kendo().TimePickerFor(model => model).HtmlAttributes(new { data_skip = "true", data_bind = "timespan:" + ViewData.TemplateInfo.GetFullHtmlFieldName(string.Empty) })
    .Format("HH:mm:ss")
)
