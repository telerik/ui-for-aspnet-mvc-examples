@model TimeSpan?

@(
        Html.Kendo().TimePicker().HtmlAttributes(new { data_skip=true, data_bind = "timespan:Time" })
        .Name("timepicker")
        .Format("HH:mm:ss")
    )
@Html.HiddenFor(model => model)

